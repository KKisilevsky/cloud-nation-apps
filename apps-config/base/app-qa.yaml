apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: search-engine-qa
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io  
spec:
  destination:
    namespace: dev
    server: https://kubernetes.default.svc
  project: dev-tenant
  source:
    path: search-engine/overlays/qa
    repoURL: https://github.com/cloud-nation/apps
    targetRevision: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
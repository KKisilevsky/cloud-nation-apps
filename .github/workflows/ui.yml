name: Build search engine ui image

on:
    workflow_dispatch:
        inputs:
          tag:
            description: 'Image tag'
            required: false
            default: 'latest'
     
    push:
        branches: [ main, dev ]
        paths:
            - 'search_engine_ui/**'

    pull_request:
        branches: [ main, dev ]
        paths:
            - 'search_engine_ui/**'
  
jobs:
    build:
      # The type of runner that the job will run on
      runs-on: ubuntu-latest

      steps:
      - uses: actions/checkout@v2

      - name: Login to GitHub Packages Docker Registry
        uses: docker/login-action@v1
        with:
          registry: docker.pkg.github.com
          username: ${{ github.repository_owner }}
          password: ${{ secrets.CI_TOKEN }}

      - name: Build and Publish Docker Image
        shell: bash
        run: |
            export IMAGETAG="docker.pkg.github.com/${{ github.repository_owner }}/${{ github.repository_name }}/search_engine_ui:${{ github.event.inputs.tag }}"
            docker build ${{ github.workspace }}/search_engine_ui -t $IMAGETAG
            docker push $IMAGETAG
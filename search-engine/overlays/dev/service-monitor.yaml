apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ui-monitor
  labels:
    component: ui
spec:
  selector:
    matchLabels:
      app: ui
  endpoints:
  - port: dev-ui-metrics
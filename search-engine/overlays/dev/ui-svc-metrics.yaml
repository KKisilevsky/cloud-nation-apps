apiVersion: v1
kind: Service
metadata:
  name: ui-svc-metrics
  labels:
    metrics: dev-ui
spec:
  selector:
    app: ui
  ports:
    - name: dev-ui-metrics
      protocol: TCP
      port: 8000
      targetPort: 8000
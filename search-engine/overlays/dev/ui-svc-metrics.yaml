apiVersion: v1
kind: Service
metadata:
  name: ui-svc-metrics
  labels:
    app: ui
spec:
  selector:
    app: ui
  ports:
    - name: dev-ui
      protocol: TCP
      port: 8000
      targetPort: 8000